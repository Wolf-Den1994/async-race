export enum ElemClasses {
  Hidden = 'hidden',
}

export enum Methods {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum StatusCar {
  Started = 'started',
  Stopped = 'stopped',
  Drive = 'drive',
}

export enum StateTable {
  Decrease = 'decrease',
  Increase = 'increase',
}

export enum Arrow {
  Up = '&#8593',
  Down = '&#8595',
}

export enum Sort {
  ID = 'id',
  WINS = 'wins',
  TIME = 'time',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum TextUnsorted {
  WINS = 'Wins',
  TIME = 'Best Time (seconds)',
}

export enum Tags {
  BUTTON = 'button',
  FOOTER = 'footer',
  HEADER = 'header',
  SECTION = 'section',
  DIV = 'div',
  INPUT = 'input',
  MAIN = 'main',
  TITLE2 = 'h2',
  TITLE3 = 'h3',
  TABLE = 'table',
  THEAD = 'thead',
  TBODY = 'tbody',
  TR = 'tr',
  TH = 'th',
}
